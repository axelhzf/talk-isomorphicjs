{
  "name": "tak-isomorphicjs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node_modules/.bin/cleaver slides.md",
    "dev": "node_modules/.bin/cleaver watch slides.md",
    "deploy": "git checkout gh-pages && git merge master && git push origin gh-pages && git checkout -"
  },
  "dependencies": {
    "cleaver": "^0.7.2"
  }
}
